<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 表单报名客户信息表(PROMOTION_ENROLL_CUSTOMER_INFO) -->
<mapper namespace="com.jubaozan.service.promotion.repository.mybatis.mapper.PromotionEnrollCustomerInfoMapper">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="promotionEnrollCustomerInfoMap" type="com.jubaozan.service.promotion.model.PromotionEnrollCustomerInfoEntity">
        <id column="ID" property="id" jdbcType="BIGINT" />
        <result column="FORM_ID" property="formId" jdbcType="BIGINT" />
        <result column="CUSTOMER_NO" property="customerNo" jdbcType="INTEGER" />
        <result column="FULL_NAME" property="fullName" jdbcType="VARCHAR" />
        <result column="MOBILE" property="mobile" jdbcType="VARCHAR" />
        <result column="REMARK" property="remark" jdbcType="VARCHAR" />
        <result column="SJID" property="sjid" jdbcType="INTEGER" />
        <result column="CREATE_TIME" property="createTime" jdbcType="TIMESTAMP" />
        <result column="UPDATE_TIME" property="updateTime" jdbcType="TIMESTAMP" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        ID, FORM_ID, CUSTOMER_NO, FULL_NAME, MOBILE, REMARK, SJID, CREATE_TIME, 
        UPDATE_TIME
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findPromotionEnrollCustomerInfoAll" resultMap="promotionEnrollCustomerInfoMap">
        SELECT
        <include refid="allColumns" />
        FROM promotion_enroll_customer_info
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findPromotionEnrollCustomerInfoByCondition" resultMap="promotionEnrollCustomerInfoMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM promotion_enroll_customer_info WHERE 1 = 1
        <if test="formId != null">
            AND FORM_ID = #{formId}
        </if>
        <if test="customerNo != null">
            AND CUSTOMER_NO = #{customerNo}
        </if>
        <if test="sjid != null">
            AND sjid = #{sjid}
        </if>
        <if test="fullName != null and fullName != ''">
            AND FULL_NAME LIKE CONCAT('%', #{fullName}, '%')
        </if>
    </select>

    <!-- 根据条件参数查询数据列表 -->
    <select id="pageQueryEnrollCustomerInfo" resultMap="promotionEnrollCustomerInfoMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM promotion_enroll_customer_info WHERE 1 = 1
        <if test="formId != null">
            AND FORM_ID = #{formId}
        </if>
        <if test="customerNo != null">
            AND CUSTOMER_NO = #{customerNo}
        </if>
        <if test="sjid != null">
            AND sjid = #{sjid}
        </if>
        <if test="startEnrollTime != null and endEnrollTime !=null">
            AND create_time BETWEEN #{startEnrollTime} AND #{endEnrollTime}
        </if>
        ORDER BY create_time DESC
        limit #{start},#{offset}
    </select>

    <select id="countEnrollCustomerInfo" resultType="INT" parameterType="map">
        SELECT
        count(*)
        FROM promotion_enroll_customer_info WHERE 1 = 1
        <if test="formId != null">
            AND FORM_ID = #{formId}
        </if>
        <if test="sjid != null">
            AND sjid = #{sjid}
        </if>
        <if test="startEnrollTime != null and endEnrollTime !=null">
            AND create_time BETWEEN #{startEnrollTime} AND #{endEnrollTime}
        </if>
    </select>

    <!-- 根据主键查询数据 -->
    <select id="findPromotionEnrollCustomerInfoByIds" resultMap="promotionEnrollCustomerInfoMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM promotion_enroll_customer_info WHERE ID IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertPromotionEnrollCustomerInfo" useGeneratedKeys="true"
            keyProperty="id" parameterType="com.jubaozan.service.promotion.model.PromotionEnrollCustomerInfoEntity">
        INSERT INTO promotion_enroll_customer_info (
            FORM_ID, CUSTOMER_NO, FULL_NAME, MOBILE, REMARK, SJID
        ) VALUES (
            #{formId},
            #{customerNo},
            #{fullName},
            #{mobile},
            #{remark},
            #{sjid}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updatePromotionEnrollCustomerInfo" parameterType="map">
        UPDATE promotion_enroll_customer_info
        <set>
            <if test="formId != null">
                FORM_ID = #{formId},
            </if>
            <if test="customerNo != null">
                CUSTOMER_NO = #{customerNo},
            </if>
            <if test="fullName != null">
                FULL_NAME = #{fullName},
            </if>
            <if test="mobile != null">
                MOBILE = #{mobile},
            </if>
            <if test="remark != null">
                REMARK = #{remark},
            </if>
            <if test="sjid != null">
                SJID = #{sjid}
            </if>
        </set>
        WHERE ID = #{id}
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deletePromotionEnrollCustomerInfoById" parameterType="long">
        DELETE FROM promotion_enroll_customer_info WHERE ID = #{id}
    </delete>

    <!--查询该表单有多少用户回答-->
    <select id="countEnrollNumByFormId" resultType="INT">
        select count(*) from promotion_enroll_customer_info where form_id=#{formId} and sjid=#{sjid}
    </select>

</mapper>