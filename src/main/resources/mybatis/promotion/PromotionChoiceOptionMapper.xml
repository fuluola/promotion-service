<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 报名表单选择题选项(PROMOTION_CHOICE_OPTION) -->
<mapper namespace="com.jubaozan.service.promotion.repository.mybatis.mapper.PromotionChoiceOptionMapper">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="promotionChoiceOptionMap" type="com.jubaozan.service.promotion.model.PromotionChoiceOptionEntity">
        <id column="ID" property="id" jdbcType="BIGINT" />
        <result column="FORM_ID" property="formId" jdbcType="BIGINT" />
        <result column="QUESTION_ID" property="questionId" jdbcType="BIGINT" />
        <result column="CHOICE_OPTION" property="choiceOption" jdbcType="VARCHAR" />
        <result column="SJID" property="sjid" jdbcType="INTEGER" />
        <result column="CREATE_TIME" property="createTime" jdbcType="TIMESTAMP" />
        <result column="UPDATE_TIME" property="updateTime" jdbcType="TIMESTAMP" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        ID, FORM_ID, QUESTION_ID, CHOICE_OPTION, SJID, CREATE_TIME, UPDATE_TIME
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findPromotionChoiceOptionAll" resultMap="promotionChoiceOptionMap">
        SELECT
        <include refid="allColumns" />
        FROM promotion_choice_option
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findPromotionChoiceOptionByCondition" resultMap="promotionChoiceOptionMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM promotion_choice_option WHERE 1 = 1
        <if test="formId != null">
            AND FORM_ID = #{formId}
        </if>
        <if test="questionId != null">
            AND QUESTION_ID = #{questionId}
        </if>
        <if test="choiceOption != null and choiceOption != ''">
            AND CHOICE_OPTION LIKE CONCAT('%', #{choiceOption}, '%')
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findPromotionChoiceOptionByIds" resultMap="promotionChoiceOptionMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM promotion_choice_option WHERE ID IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertPromotionChoiceOption" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.jubaozan.service.promotion.model.PromotionChoiceOptionEntity">
        INSERT INTO promotion_choice_option (
            FORM_ID, QUESTION_ID, CHOICE_OPTION, SJID
        ) VALUES (
            #{formId},
            #{questionId},
            #{choiceOption},
            #{sjid}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updatePromotionChoiceOption" parameterType="map">
        UPDATE promotion_choice_option
        <set>
            <if test="formId != null">
                FORM_ID = #{formId},
            </if>
            <if test="questionId != null">
                QUESTION_ID = #{questionId},
            </if>
            <if test="choiceOption != null">
                CHOICE_OPTION = #{choiceOption}
            </if>

        </set>
        WHERE ID = #{id}
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deletePromotionChoiceOptionById" parameterType="long">
        DELETE FROM promotion_choice_option WHERE ID = #{id}
    </delete>
    
</mapper>