spring:
  application:
    name: promotion-service
  cloud:
    consul:
      config:
        prefix: config
        format: YAML
      host: consul
      port: 8500
      enabled: true
      discovery:
        enabled: true
        register: true
        prefer-ip-address: true
swagger:
    # 配置需要生成文档的包路径
    base-package: com.jubaozan.service.promotion.controller  

